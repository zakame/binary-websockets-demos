# WebSockets demo for Java (using Maven)

This needs [Apache Maven](https://maven.apache.org/) for building a
standalone jar:

    $ mvn clean package
    $ java -jar target/binaryws-1.0-SNAPSHOT-jar-with-dependencies.jar
